applications:
  - name: sfccmstest
    memory: 1G
    disk_quota: 2GB
    buildpack: https://github.com/cloudfoundry/nodejs-buildpack.git
    services:
      - sfccmstest-db
      - sfccmstest-s3
      - sfccmstest-redis-new
    env:
      CORS_ORIGIN: "true"
      CORS_ENABLED: "true"
      CORS_METHODS: "GET"
stack: cflinuxfs3
